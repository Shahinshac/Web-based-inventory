# Server Environment Variables

# MongoDB Connection (Get from MongoDB Atlas)
MONGODB_URI=mongodb+srv://shahinshac123_db_user:<shahinsha2007@cluster0.majmsqd.mongodb.net/?appName=Cluster0
DB_NAME=inventorydb

# Admin Credentials (Change these!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here

# Set to true only if you want to allow changing the admin password via the API
# This is disabled by default for better security. Recommended: leave false and
# manage admin password via deployment/secret store.
ALLOW_ADMIN_PASSWORD_CHANGE=false

# Image Fetching API (Optional - for auto product photos)
# Get free API key from https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=your-unsplash-access-key-here

# Server Configuration
PORT=4000
NODE_ENV=development

# Email functionality has been removed/disabled in this deployment.
# If you want to re-enable email features (OTP, invoice emails) consider
# adding secure credentials and a reliable sender via a secret store.
